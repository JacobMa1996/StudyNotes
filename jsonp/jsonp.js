const http = require('http')

const server = http.createServer((req, res) => {
    let url = req.url
    if (url == '/jsonp') {
        // let callback = 
    }
})

function query (name, url) {
    
}